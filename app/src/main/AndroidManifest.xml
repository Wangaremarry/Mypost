<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mary.dev.mypost2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPost2">
        <activity
            android:name=".CommentRVAdapter"
            android:exported="false" />
        <activity
            android:name=".PostRVActivity"
            android:exported="false" />
        <activity
            android:name=".CommentActivity"
            android:exported="false"
            android:label="@string/title_activity_comment"
            android:theme="@style/Theme.MyPost2.NoActionBar" />
        <activity
            android:name=".PostRVAdapter"
            android:exported="false"
            android:label="@string/title_activity_post_rvadapter"
            android:theme="@style/Theme.MyPost2.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>